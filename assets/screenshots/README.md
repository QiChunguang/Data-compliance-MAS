# Screenshots

This folder stores sanitized product screenshots used by the README.

Expected files:

- `interface_home.png`
- `report_generation.png`
- `knowledge_graph.png`

Screenshots must not show secrets, real user data, private local paths, private IPs, backend error banners, debug panels, runtime manifests, or raw internal scoring artifacts.
